services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    volumes:
      - ./server:/app
      - /app/node_modules
    env_file:
      - ./server/.env
    environment:
      - PORT=4000
      - ALLOWED_ORIGIN=http://localhost:3000
    command: npm run dev